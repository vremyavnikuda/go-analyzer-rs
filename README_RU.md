# Go Analyzer RS

[README (English)](README.md)

**Готовый к использованию анализатор Go-кода** - Анализатор Go-кода на Rust с поддержкой VS Code, обеспечивающий интеллектуальное отслеживание жизненного цикла переменных и продвинутое обнаружение гонок данных.

✅ **[25.08.2025](https://github.com/vremyavnikuda/go-analyzer-rs/commit/d24b2cb42eb70ef22012726f390137cd8b0b26a9)**: Стабильный, устойчивый к сбоям LSP-сервер с расширенными возможностями анализа

## Возможности

### **Улучшенный анализ переменных**

- **Поддержка всех конструкций Go**: Переменные, поля структур, методы интерфейсов, параметры функций
- **Продвинутое определение области видимости**: Функциональный уровень, переменные циклов, type switch, range clause
- **Интеллектуальное позиционирование курсора**: Контекстно-зависимое определение с 15+ специализированными контекстами
- **Автоматический анализ в реальном времени**: Анализ с задержкой и настраиваемой конфигурацией (300мс по умолчанию)

### **Стабилизировано**

- **Устойчивый к сбоям LSP-сервер**: Комплексное восстановление после паник и обработка ошибок
- **Безопасные операции с памятью**: Правильное управление асинхронными ресурсами и очистка
- **Плавная деградация**: Безопасные fallback'и при возникновении ошибок анализа
- **Оптимизированная производительность**: Инкрементальный парсинг с кэшированием AST

### **Стабилизировано: обнаружение гонок**

- **Контекстно-зависимые уровни серьёзности**: Обнаружение гонок с приоритетом Высокий/Средний/Низкий
- **Осведомлённость о синхронизации**: Обнаруживает блокировки мьютексов и атомарные операции
- **Умный анализ горутин**: Множественные паттерны горутин (анонимные функции, вызовы методов)
- **Анализ доступа к переменным**: Обнаружение операций Чтение/Запись/Модификация/Взятие адреса

### **Улучшенная визуализация**

- **Управление расширением**: Быстрая активация/деактивация с помощью горячих клавиш (Shift+Alt+S/C) и визуальных индикаторов статуса
- **Цветовой анализ**:
  - 🟢 **Зелёный**: Объявления переменных
  - 🟡 **Жёлтый**: Обычное использование переменных
  - 🔵 **Синий**: Операции с указателями (`&var`, `*ptr`)
  - 🔴 **Красный**: Высокоприоритетные гонки данных в горутинах
  - 🟠 **Оранжевый**: Низкоприоритетные гонки (синхронизированные контексты)
  - 🟣 **Фиолетовый**: Переопределённые псевдонимы переменных
  - 🟪 **Пурпурный**: Захваченные переменные в замыканиях
- **Богатая информация при наведении**: Подробный жизненный цикл переменных и предупреждения о гонках
- **Интеллектуальная подсветка**: Контекстно-чувствительное выделение на основе позиции курсора

## Установка

> **Примечание**: LSP-сервер тщательно протестирован и включает комплексную обработку ошибок для обеспечения стабильности во время анализа.

> **Примечание**: LSP-сервер тщательно протестирован и включает комплексную обработку ошибок для обеспечения стабильности во время анализа.

### Быстрый старт (Рекомендуется)

Используйте предоставленный Makefile для простой сборки и упаковки:

```bash
# Собрать всё и упаковать расширение (Windows)
make build-windows

# Собрать всё и упаковать расширение (Linux/macOS)
make build-linux

# Протестировать сборку
cargo test
```

#### Пошаговая сборка:

```bash
make clean    # Очистить предыдущие сборки (win-clean или unix-clean)
# Собрать всё и упаковать расширение (Windows)
make build-windows

# Собрать всё и упаковать расширение (Linux/macOS)
make build-linux

# Протестировать сборку
cargo test
```

#### Пошаговая сборка:

```bash
make clean    # Очистить предыдущие сборки (win-clean или unix-clean)
make build    # Собрать Rust сервер
make copy     # Скопировать сервер в папку расширения
make npm      # Установить Node.js зависимости
make compile  # Скомпилировать TypeScript расширение
make package  # Упаковать VS Code расширение
```

### Ручная установка

#### Сборка сервера

```bash
cargo build --release
```

#### Сборка VS Code расширения

```bash
cd vscode
npm install
npm run compile
```

#### Копирование бинарника сервера

После сборки Rust сервера скопируйте бинарник в папку расширения:

```bash
# Создать папку server если её нет
mkdir -p vscode/server

# Скопировать бинарник (Windows)
copy target\release\go-analyzer-rs.exe vscode\server\

# Скопировать бинарник (Linux/macOS)
cp target/release/go-analyzer-rs vscode/server/
```

### Сборка для разработки

Для разработки можно использовать:

```bash
# Собрать в режиме отладки
cargo build

# Собрать расширение в режиме наблюдения
cd vscode && npm run watch
```

## Использование

### Управление активацией расширения

**Горячие клавиши**:

- **Shift+Alt+S**: Активировать расширение (включить анализ)
- **Shift+Alt+C**: Деактивировать расширение (выключить анализ)

**Визуальный индикатор статуса**: В строке состояния отображается текущее состояние расширения:

- ✅ **Активно**: Расширение работает и анализирует код
- ❌ **Неактивно**: Расширение отключено для экономии ресурсов

> **Примечание**: Расширение не всегда должно быть активным. Используйте деактивацию, когда хотите сэкономить системные ресурсы или сосредоточиться на других задачах.

### Автоматический анализ (когда активно)

1. Откройте Go файл в VS Code
2. Установите расширение (если еще не установлено)
3. Убедитесь, что расширение активно (✅ в строке состояния или используйте Shift+Alt+S)
4. Просто перемещайте курсор по переменным - анализ запустится автоматически
5. Увидите цветовую индикацию всего жизненного цикла переменной

### Ручной анализ

1. Выберите переменную в коде
2. Выполните команду `Go Analyzer: Show Lifecycle` (Ctrl+Shift+P → "Go Analyzer: Show Lifecycle")

## Настройки

В настройках VS Code можно настроить:

- `goAnalyzer.enableAutoAnalysis` - включить/выключить автоматический анализ (по умолчанию: true)
- `goAnalyzer.autoAnalysisDelay` - задержка перед автоматическим анализом в миллисекундах (по умолчанию: 300)
- `goAnalyzer.declarationColor` - цвет объявлений переменных (по умолчанию: "green")
- `goAnalyzer.useColor` - цвет использования переменных (по умолчанию: "yellow")
- `goAnalyzer.pointerColor` - цвет операций с указателями (по умолчанию: "blue")
- `goAnalyzer.raceColor` - цвет предупреждений о data race (по умолчанию: "red")
- `goAnalyzer.raceLowColor` - цвет низкоприоритетных предупреждений о гонках (по умолчанию: "orange")
- `goAnalyzer.aliasReassignedColor` - цвет переопределённых псевдонимов переменных (по умолчанию: "purple")
- `goAnalyzer.aliasCapturedColor` - цвет захваченных переменных в замыканиях (по умолчанию: "magenta")

## Пример

``go
func main() {
x := 42 // 🟢 Объявление
println(x) // 🟡 Использование
x = 100 // 🟣 Переопределение
ptr := &x // 🟡 Использование x, 🟢 Объявление ptr
println(\*ptr) // 🔵 Использование указателя
go func() {
println(x) // 🟪 Захваченная переменная в горутине
}()
}

````

## Технические детали

### Архитектура

- **Сервер**: Rust LSP-сервер с парсингом Go через tree-sitter
- **Клиент**: TypeScript расширение для VS Code
- **Протокол**: Language Server Protocol (LSP) через tower-lsp
- **Рантайм**: Tokio асинхронный рантайм для производительности
- **Парсинг**: Инкрементальный парсинг AST с кэшированием

### Улучшенные возможности (25.08.2025)

- **Продвинутая поддержка Go-конструкций**: 10+ паттернов объявления переменных
- **Умное определение курсора**: 15+ типов контекста для точного анализа
- **Надёжная обработка ошибок**: Восстановление после паник с плавной деградацией
- **Безопасное многопоточность**: Правильное управление асинхронными мьютексами
- **Контекстно-зависимое обнаружение гонок**: Уровни серьёзности основанные на синхронизации

### Возможности производительности

- **Анализ с задержкой**: Настраиваемая задержка (300мс по умолчанию) предотвращает перегрузку
- **Умное кэширование**: Повторное использование спарсенных AST-деревьев для эффективности
- **Инкрементальный парсинг**: Переанализ только изменённых частей
- **Очистка ресурсов**: Правильное удаление декораций и обработчиков событий

### Поддерживаемые паттерны Go

- Объявления переменных: `var x int`, `x := value`
- Параметры функций и возвращаемые значения
- Доступ к полям структур: `obj.field`
- Вызовы методов интерфейсов: `interface.method()`
- Range clauses: `for i, v := range slice`
- Type switches: `switch v := x.(type)`
- Анализ горутин: `go func(){}()`, `go method()`
- Операции с указателями: `&var`, `*ptr`

## Обеспечение качества

### Тестирование

Проект включает комплексные юнит-тесты, покрывающие все функции анализа:

```bash
# Запустить все тесты
cargo test

# Запустить конкретный тест
cargo test test_enhanced_cursor_position_detection

# Проверить ошибки компиляции
cargo check
````

**Покрытие тестами**:

- ✅ Точность обнаружения переменных
- ✅ Определение позиции курсора
- ✅ Анализ горутин
- ✅ Обнаружение синхронизации
- ✅ Определение серьёзности гонок
- ✅ Подсчёт сущностей
- ✅ Улучшенный анализ контекста курсора

### Функции стабильности

- **Восстановление после паник**: Все функции анализа включают защиту `catch_unwind`
- **Управление ресурсами**: Правильная обработка асинхронных мьютексов и очистка
- **Логирование ошибок**: Комплексное сообщение об ошибках для отладки
- **Плавная деградация**: Безопасные fallback'и при возникновении проблем анализа

## Разработка

### Структура проекта

```
go-analyzer-rs/
├── src/
│   └── main.rs          # Rust LSP сервер
├── vscode/
│   └── src/
│       └── extension.ts # VS Code расширение
├── go_test/
│   └── main.go          # Тестовый Go файл
└── Cargo.toml
```

### Запуск в режиме разработки

#### Использование Makefile (Рекомендуется)

```bash
# Полная сборка для разработки
make rebuild

# Или пошагово:
make clean
make build
make copy
make npm
make compile
```

#### Ручная настройка для разработки

1. Соберите сервер: `cargo build`
2. Скопируйте бинарник: `copy target\debug\go-analyzer-rs.exe vscode\server\` (Windows) или `cp target/debug/go-analyzer-rs vscode/server/` (Linux/macOS)
3. Соберите расширение: `cd vscode && npm run compile`
4. Запустите VS Code в режиме отладки (F5)
5. Откройте тестовый Go файл и протестируйте функциональность

## Как это работает

### Анализ жизненного цикла переменных

Анализатор использует tree-sitter для парсинга Go-кода и построения Абстрактного Синтаксического Дерева (AST). Когда переменная выбрана (либо в объявлении, либо в использовании), система:

1. **Находит объявление переменной** в текущей области видимости
2. **Собирает все использования** по всей функции
3. **Обнаруживает операции с указателями** (взятие адреса `&`, разыменование `*`)
4. **Идентифицирует использование в горутинах** для обнаружения гонок данных
5. **Применяет цветовые декорации** для визуализации жизненного цикла

### Анализ в реальном времени

- **Отслеживание курсора**: Мониторинг изменений позиции курсора в Go файлах
- **Запросы с задержкой**: Использует настраиваемую задержку для предотвращения избыточных запросов к серверу
- **Умные обновления**: Анализ только при реальном изменении позиции курсора
- **Управление ресурсами**: Правильная очистка декораций и таймаутов

### Обнаружение гонок данных

Анализатор обнаруживает потенциальные гонки данных через:

1. **Идентификацию горутин**: Поиск операторов `go` в коде
2. **Использование переменных в горутинах**: Проверка используются ли переменные внутри горутин
3. **Анализ области видимости**: Определение разделяются ли переменные между горутинами
4. **Визуальные предупреждения**: Маркировка таких использований красным цветом

## Соображения производительности

- **Эффективный парсинг**: Использует tree-sitter для быстрого инкрементального парсинга
- **Анализ с задержкой**: Настраиваемая задержка предотвращает избыточное использование CPU
- **Умное кэширование**: Повторное использование спарсенного AST когда возможно
- **Очистка ресурсов**: Правильное удаление декораций и обработчиков событий

## Устранение неполадок

### Частые проблемы

1. **Декорации не появляются**:

   - Проверьте, что файл имеет расширение `.go`
   - Убедитесь, что LSP-сервер работает
   - Проверьте консоль VS Code на ошибки

2. **Медленная производительность**:

   - Увеличьте `autoAnalysisDelay` в настройках
   - Отключите автоматический анализ если не нужен
   - Проверьте большие файлы или сложный код

3. **Неправильный анализ**:
   - Убедитесь, что синтаксис Go правильный
   - Проверьте ошибки парсинга в консоли
   - Проверьте совместимость грамматики tree-sitter

## Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для функционала
3. Внесите изменения
4. Добавьте тесты при необходимости
5. Отправьте pull request

## Лицензия

Этот проект лицензирован под лицензией MIT - см. файл LICENSE для деталей.
